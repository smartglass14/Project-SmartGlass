import { Schema } from 'mongoose';

export const optionSchema = new Schema({
  text: {
    type: String,
    required: true,
    trim: true,
  },
  votes: {
    type: Number,
    default: 0,
  },
});
